'use client';

import { usePreview } from '../../lib/sanity.preview';

type Props = {
  queryHome: string;
};

export default function PreviewHomePage({ queryHome }: Props) {
  // const homeData = usePreview(null, queryHome);
  return <></>;
}
